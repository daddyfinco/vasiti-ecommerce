 <section>
      <div class="container">
        <div class="row">
          <!--.col-sm-3-->
          <!--    .left-sidebar-->
          <!--        h2 Categories-->
          <!--        //category-products-->
          <!--        #accordian.panel-group.category-products-->
          <!--            if categories-->
          <!--                each category in categories-->
          <!--                    .panel.panel-default-->
          <!--                        .panel-heading-->
          <!--                            h4.panel-title-->
          <!--                                a(href='/categories/' + category.CategorySlug)= category.CategoryName-->
          <!--        ///category-products-->
          <!--.col-sm-9.padding-right-->
          <!--features_items-->
          <div class="features_items">
            <h2 class="title text-center">Featured Products</h2>{{#if featProducts}}
              {{#each featProducts as |product|}}
                <div class="col-sm-3">
                  <div class="product-image-wrapper">
                    <div class="single-products">
                      <div class="productinfo text-center"><a href="{{concat '/cat/'  product.CategorySlug '/' product.ProductSlug}}"><img src="/img/products/{{product.Image}}" alt="{{product.product_name}}"></a>
                        <p><a href="{{concat '/cat/'  product.CategorySlug '/' product.ProductSlug}}">{{product.product_name}}</a></p>
                        <h2>{{concat '₦' product.ProductPrice}}</h2>
                        <form action="/cart/{{product.ProductID}}/add" method="POST"><button type="submit" class="btn btn-default add-to-cart"><i class="fa fa-shopping-cart"></i>Add to cart</button></form>
                      </div>
                    </div>
                  </div>
                </div>
              {{/each}}{{/if}}
          </div>
          <!--/features_items-->
        </div>
        <div class="row">
          <div class="features_items">
            <h2 class="title text-center">Product Categories</h2>{{#if categories}}
              {{#each categories as |category|}}
                <div class="col-sm-3"><a href="/cat/{{category.CategorySlug}}">
                    <div class="product-image-wrapper">
                      <div class="single-products">
                        <div class="productinfo text-center"><img src="/img/categories/{{category.Image}}" alt="{{category.CategoryName}}">
                          <h2>{{category.CategoryName}}</h2>
                        </div>
                      </div>
                    </div>
                  </a></div>
              {{/each}}{{/if}}
          </div>
        </div>
      </div>
    </section>
